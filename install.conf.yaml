- link:
    ~/.Xresources:
    ~/.config/fontconfig/fonts.conf:
    ~/.config/nvim:
    ~/.dovecot.sieve:
    ~/.gdbinit:
    ~/.gitconfig:
    ~/.hgrc:
    ~/.mutt.colors:
    ~/.muttrc:
    ~/.notmuch-config:
    ~/.signature:
    ~/.tmux.conf:
    ~/.toprc:
    ~/.zprofile: zsh/zprofile
    ~/.zsh: zsh/zsh
    ~/.zshenv: zsh/zshenv
    ~/.zshrc: zsh/zshrc
    ~/bin/proxy:
    ~/.config/sway:
    ~/.config/swaylock:
    ~/.config/waybar:

- clean:
    ~/:
    ~/bin:
    ~/.config:
      recursive: true
